
import { checkmark } from 'ember-test/helpers/checkmark';
import { module, test } from 'qunit';

module('Unit | Helper | checkmark');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkmark([42]);
  assert.ok(result);
});

